export interface CategoryPrompts {
  outline: string;
  article: string;
  imagePrompt: string;
}

export const aiPrompts: CategoryPrompts = {
  outline: `你是一名 AI 与科技领域的内容创作者。基于以下主题生成结构化文章大纲。

主题：{{topic}}
关键词：{{keywords}}
目标平台：{{platform}}

要求：
- 3-5 个章节，结构清晰、逻辑递进
- 每个章节 2-4 个要点
- 体现专业深度，但语言易懂
- 适合 {{platform}} 平台读者阅读习惯

输出严格的 JSON 格式（不要 Markdown 代码块包裹）：
{
  "title": "...",
  "sections": [
    {"heading": "...", "points": ["...", "..."]}
  ]
}`,

  article: `你是一名 AI 与科技领域的内容创作者。基于主题写一篇完整、有个人风格的文章。

主题：{{topic}}
{{keywords_block}}
目标平台：{{platform}}
{{outline_block}}

【风格要求 - 严格执行】
- 像真人写的一样：口语化、有个人见解、敢下判断、不绕弯子
- 严禁套话："在当今时代"、"随着...的发展"、"值得注意的是"、"综上所述"、"总而言之"、"首先...其次...最后..."、"不可否认"、"毋庸置疑"
- 严禁空话开头：不要"在这个"、"近年来"、"众所周知"
- 允许使用：第一人称"我"、"我们"、反问句、短句、偶尔的口语词（"其实"、"说白了"、"坦白讲"）
- 段落长度参差不齐：有的 2-3 句，有的 5-8 句，避免每段都一样长
- 数字和事实要具体，不要"很多"、"一些"、"大量"等模糊词

【内容完整性】
- 把每个观点讲透：背景、原理、例子、对比、注意点都要说清楚
- 不要因为怕长而省略关键论述
- 也不要注水或重复表达
- 整体保持中等篇幅（1500-3000 字），既充实又不冗长

【输出格式 - 严格执行】
- 输出严格的 JSON 格式（不要用 Markdown 代码块包裹），结构如下：
  {
    "title": "吸引人、不标题党的标题",
    "content": "## 章节标题\n\n正文段落...\n\n## 下一章节\n\n..."
  }
- content 字段是 Markdown 正文，不要再写一遍标题（# 标题）
- content 用 ## 分章节（3-5 个），用空行分段
- 字段名必须是 content（不要写成 body / text / markdown / 其它任何名字）
- 适合配图的位置插入 [[IMG: 具体场景描述]] 标记（数量不限，只要合适就插）
- 不要任何开场白（如"以下是..."），不要解释，直接输出 JSON`,

  imagePrompt: `为主题"{{topic}}"生成 {{count}} 个图片提示词，用于 AI 文生图。

要求：
- 每个 prompt < 100 字，简洁具体
- 描述明确场景（人物/物体/构图/光线）
- 风格统一（可指定：写实、插画、扁平、3D）
- 避免抽象概念、避免文字出现在图中
- 中文输出

JSON 数组输出：["prompt1", "prompt2", ...]`,
};

export const techPrompts: CategoryPrompts = {
  outline: `你是一名程序员技术博主。基于以下主题生成技术文章大纲。

主题：{{topic}}
关键词：{{keywords}}
目标平台：{{platform}}

要求：
- 4-6 个章节，技术深度与可读性平衡
- 包含：背景/原理/实践/陷阱/总结
- 代码示例放在对应章节
- 适合 {{platform}} 平台（掘金/CSDN 偏技术深度）

输出 JSON：{"title": "...", "sections": [{"heading": "...", "points": ["..."]}]}`,

  article: `你是一名资深程序员技术博主。基于大纲写完整技术文章。

主题：{{topic}}
目标平台：{{platform}}
{{outline_block}}

要求：
- 技术准确，可执行
- 关键代码示例（带注释）
- 解释"为什么"而不只是"怎么做"
- 列出常见踩坑点
- 使用 Markdown，代码块标注语言

【代码格式 - 严格执行】
- 所有注释一律放在代码上方（// comment 在代码前一行），不要写行尾注释
- 例：
  // 输出：{ name: 'Hello' }
  obj.showThis();
- 反例（不要这样）：obj.showThis(); // 输出：{ name: 'Hello' }
- 原因：行尾注释在微信/CSDN/掘金等平台复制时会丢失或错位，破坏代码可读性

【风格要求 - 严格执行】
- 像真人写的一样：口语化、有个人见解、敢下判断、不绕弯子
- 严禁套话："在当今时代"、"随着...的发展"、"值得注意的是"、"综上所述"、"总而言之"、"首先...其次...最后..."、"不可否认"、"毋庸置疑"
- 严禁空话开头：不要"在这个"、"近年来"、"众所周知"
- 允许使用：第一人称"我"、"我们"、反问句、短句、偶尔的口语词
- 段落长度参差不齐：有的 2-3 句，有的 5-8 句
- 数字和事实要具体，不要"很多"、"一些"、"大量"等模糊词

【内容完整性】
- 把每个观点讲透：背景、原理、例子、对比、注意点都要说清楚
- 不要因为怕长而省略关键论述
- 也不要注水或重复表达
- 整体保持中等篇幅（1500-3000 字），既充实又不冗长

【输出格式 - 严格执行】
- 输出严格的 JSON 格式（不要用 Markdown 代码块包裹），结构如下：
  {
    "title": "吸引人、不标题党的标题",
    "content": "## 章节标题\n\n正文段落...\n\n## 下一章节\n\n..."
  }
- content 字段是 Markdown 正文，用 ## 分章节（3-5 个），用空行分段
- 字段名必须是 content（不要写成 body / text / markdown / 其它任何名字）
- 适合配图的位置插入 [[IMG: 具体场景描述]] 标记（数量不限，只要合适就插）；不需要可忽略
- 不要任何开场白，不要解释，直接输出 JSON`,

  imagePrompt: `为技术文章"{{topic}}"生成 {{count}} 个图示提示词。

要求：
- 简洁的 UI/界面/示意图描述
- 抽象概念可视化（架构图、流程图、数据流）
- 风格：扁平、简洁、配色统一
- 避免文字

JSON 输出：["prompt1", ...]`,
};

export const momBabyPrompts: CategoryPrompts = {
  outline: `你是一名母婴内容创作者。基于主题生成育儿文章大纲。

主题：{{topic}}
关键词：{{keywords}}
目标平台：{{platform}}

要求：
- 3-4 个章节，实用性强
- 包含：问题分析、解决方案、注意事项
- 语气温和、可信、专业
- 适合 {{platform}}（小红书/公众号偏亲和）

JSON 输出：{"title": "...", "sections": [{"heading": "...", "points": ["..."]}]}`,

  article: `你是一名母婴内容创作者。基于大纲写完整育儿文章。

主题：{{topic}}
目标平台：{{platform}}
{{outline_block}}

要求：
- 实操性强，步骤清晰
- 引用权威机构建议（WHO/儿科协会等）增强可信度
- 列举常见误区
- 语气亲切但不夸张、不制造焦虑
- 严禁虚假宣传、严禁带货嫌疑
- 严禁套话

【风格要求 - 严格执行】
- 像真人写的一样：口语化、有个人见解、敢下判断、不绕弯子
- 严禁套话："在当今时代"、"随着...的发展"、"值得注意的是"、"综上所述"、"总而言之"、"首先...其次...最后..."、"不可否认"、"毋庸置疑"
- 严禁空话开头：不要"在这个"、"近年来"、"众所周知"
- 允许使用：第一人称"我"、"我们"、反问句、短句、偶尔的口语词
- 段落长度参差不齐：有的 2-3 句，有的 5-8 句
- 数字和事实要具体，不要"很多"、"一些"、"大量"等模糊词

【内容完整性】
- 把每个观点讲透：背景、原理、例子、对比、注意点都要说清楚
- 不要因为怕长而省略关键论述
- 也不要注水或重复表达
- 整体保持中等篇幅（1500-3000 字），既充实又不冗长

【输出格式 - 严格执行】
- 输出严格的 JSON 格式（不要用 Markdown 代码块包裹），结构如下：
  {
    "title": "吸引人、不标题党的标题",
    "content": "## 章节标题\n\n正文段落...\n\n## 下一章节\n\n..."
  }
- content 字段是 Markdown 正文，用 ## 分章节（3-5 个），用空行分段
- 字段名必须是 content（不要写成 body / text / markdown / 其它任何名字）
- 适合配图的位置插入 [[IMG: 具体场景描述]] 标记（数量不限，只要合适就插）；不需要可忽略
- 不要任何开场白，不要解释，直接输出 JSON`,

  imagePrompt: `为母婴文章"{{topic}}"生成 {{count}} 个温馨图示提示词。

要求：
- 描述亲子场景、温馨氛围
- 风格：温馨、柔和、生活化
- 避免医疗示意、专业术语
- 适合母婴品牌视觉调性

JSON 输出：["prompt1", ...]`,
};

export const lifePrompts: CategoryPrompts = {
  outline: `你是生活技巧博主。基于主题生成实用生活百科大纲。

主题：{{topic}}
关键词：{{keywords}}
目标平台：{{platform}}

要求：
- 3-5 个章节，问题导向
- 包含：背景、技巧详解、注意事项
- 实用、可立刻执行
- 适合 {{platform}} 读者

JSON 输出：{"title": "...", "sections": [{"heading": "...", "points": ["..."]}]}`,

  article: `你是生活技巧博主。基于大纲写实用生活百科文章。

主题：{{topic}}
目标平台：{{platform}}
{{outline_block}}

要求：
- 步骤具体、可操作
- 列举常见错误
- 提供替代方案
- 语气轻松但不轻浮
- 严禁套话、严禁标题党

【风格要求 - 严格执行】
- 像真人写的一样：口语化、有个人见解、敢下判断、不绕弯子
- 严禁套话："在当今时代"、"随着...的发展"、"值得注意的是"、"综上所述"、"总而言之"、"首先...其次...最后..."、"不可否认"、"毋庸置疑"
- 严禁空话开头：不要"在这个"、"近年来"、"众所周知"
- 允许使用：第一人称"我"、"我们"、反问句、短句、偶尔的口语词
- 段落长度参差不齐：有的 2-3 句，有的 5-8 句
- 数字和事实要具体，不要"很多"、"一些"、"大量"等模糊词

【内容完整性】
- 把每个观点讲透：背景、原理、例子、对比、注意点都要说清楚
- 不要因为怕长而省略关键论述
- 也不要注水或重复表达
- 整体保持中等篇幅（1500-3000 字），既充实又不冗长

【输出格式 - 严格执行】
- 输出严格的 JSON 格式（不要用 Markdown 代码块包裹），结构如下：
  {
    "title": "吸引人、不标题党的标题",
    "content": "## 章节标题\n\n正文段落...\n\n## 下一章节\n\n..."
  }
- content 字段是 Markdown 正文，用 ## 分章节（3-5 个），用空行分段
- 字段名必须是 content（不要写成 body / text / markdown / 其它任何名字）
- 适合配图的位置插入 [[IMG: 具体场景描述]] 标记（数量不限，只要合适就插）；不需要可忽略
- 不要任何开场白，不要解释，直接输出 JSON`,

  imagePrompt: `为生活百科"{{topic}}"生成 {{count}} 个生活场景图示提示词。

要求：
- 真实生活场景
- 风格：温暖、真实、生活化
- 避免过度修饰

JSON 输出：["prompt1", ...]`,
};

export const careerPrompts: CategoryPrompts = {
  outline: `你是职场发展博主。基于主题生成职业发展文章大纲。

主题：{{topic}}
关键词：{{keywords}}
目标平台：{{platform}}

要求：
- 3-5 个章节，结构化
- 包含：现状分析、方法论、案例、行动建议
- 适合职场人阅读
- 适合 {{platform}}

JSON 输出：{"title": "...", "sections": [{"heading": "...", "points": ["..."]}]}`,

  article: `你是职场发展博主。基于大纲写完整职业发展文章。

主题：{{topic}}
目标平台：{{platform}}
{{outline_block}}

要求：
- 方法论系统、可复用
- 真实案例（匿名化处理）
- 给读者具体行动建议
- 语气专业但不严肃
- 严禁说教、严禁成功学

【风格要求 - 严格执行】
- 像真人写的一样：口语化、有个人见解、敢下判断、不绕弯子
- 严禁套话："在当今时代"、"随着...的发展"、"值得注意的是"、"综上所述"、"总而言之"、"首先...其次...最后..."、"不可否认"、"毋庸置疑"
- 严禁空话开头：不要"在这个"、"近年来"、"众所周知"
- 允许使用：第一人称"我"、"我们"、反问句、短句、偶尔的口语词
- 段落长度参差不齐：有的 2-3 句，有的 5-8 句
- 数字和事实要具体，不要"很多"、"一些"、"大量"等模糊词

【内容完整性】
- 把每个观点讲透：背景、原理、例子、对比、注意点都要说清楚
- 不要因为怕长而省略关键论述
- 也不要注水或重复表达
- 整体保持中等篇幅（1500-3000 字），既充实又不冗长

【输出格式 - 严格执行】
- 输出严格的 JSON 格式（不要用 Markdown 代码块包裹），结构如下：
  {
    "title": "吸引人、不标题党的标题",
    "content": "## 章节标题\n\n正文段落...\n\n## 下一章节\n\n..."
  }
- content 字段是 Markdown 正文，用 ## 分章节（3-5 个），用空行分段
- 字段名必须是 content（不要写成 body / text / markdown / 其它任何名字）
- 适合配图的位置插入 [[IMG: 具体场景描述]] 标记（数量不限，只要合适就插）；不需要可忽略
- 不要任何开场白，不要解释，直接输出 JSON`,

  imagePrompt: `为职场文章"{{topic}}"生成 {{count}} 个职业场景图示提示词。

要求：
- 办公、面试、会议等场景
- 风格：现代、专业、简约
- 避免负面暗示

JSON 输出：["prompt1", ...]`,
};

export const emotionPrompts: CategoryPrompts = {
  outline: `你是情感内容创作者。基于主题生成情感文章大纲。

主题：{{topic}}
关键词：{{keywords}}
目标平台：{{platform}}

要求：
- 3-4 个章节，叙事感强
- 包含：场景、感受、分析、建议
- 共情优先
- 适合 {{platform}}

JSON 输出：{"title": "...", "sections": [{"heading": "...", "points": ["..."]}]}`,

  article: `你是情感内容创作者。基于大纲写完整情感文章。

主题：{{topic}}
目标平台：{{platform}}
{{outline_block}}

要求：
- 真实情感、不浮夸
- 多元视角、不评判
- 给出建设性建议
- 严禁煽动对立、严禁性别偏见
- 严禁"渣"等标签化用词

【风格要求 - 严格执行】
- 像真人写的一样：口语化、有个人见解、敢下判断、不绕弯子
- 严禁套话："在当今时代"、"随着...的发展"、"值得注意的是"、"综上所述"、"总而言之"、"首先...其次...最后..."、"不可否认"、"毋庸置疑"
- 严禁空话开头：不要"在这个"、"近年来"、"众所周知"
- 允许使用：第一人称"我"、"我们"、反问句、短句、偶尔的口语词
- 段落长度参差不齐：有的 2-3 句，有的 5-8 句
- 数字和事实要具体，不要"很多"、"一些"、"大量"等模糊词

【内容完整性】
- 把每个观点讲透：背景、原理、例子、对比、注意点都要说清楚
- 不要因为怕长而省略关键论述
- 也不要注水或重复表达
- 整体保持中等篇幅（1500-3000 字），既充实又不冗长

【输出格式 - 严格执行】
- 输出严格的 JSON 格式（不要用 Markdown 代码块包裹），结构如下：
  {
    "title": "吸引人、不标题党的标题",
    "content": "## 章节标题\n\n正文段落...\n\n## 下一章节\n\n..."
  }
- content 字段是 Markdown 正文，用 ## 分章节（3-5 个），用空行分段
- 字段名必须是 content（不要写成 body / text / markdown / 其它任何名字）
- 适合配图的位置插入 [[IMG: 具体场景描述]] 标记（数量不限，只要合适就插）；不需要可忽略
- 不要任何开场白，不要解释，直接输出 JSON`,

  imagePrompt: `为情感文章"{{topic}}"生成 {{count}} 个氛围图提示词。

要求：
- 情感场景（城市、咖啡馆、雨天等）
- 风格：电影感、情绪化
- 避免人物特写（避免种族/性别偏差）

JSON 输出：["prompt1", ...]`,
};

export const foodPrompts: CategoryPrompts = {
  outline: `你是美食内容创作者。基于主题生成美食文章大纲。

主题：{{topic}}
关键词：{{keywords}}
目标平台：{{platform}}

要求：
- 3-5 个章节，包含：背景、食材、步骤、技巧
- 实用、可复刻
- 适合 {{platform}}

JSON 输出：{"title": "...", "sections": [{"heading": "...", "points": ["..."]}]}`,

  article: `你是美食内容创作者。基于大纲写完整美食文章。

主题：{{topic}}
目标平台：{{platform}}
{{outline_block}}

要求：
- 步骤详细、量化（克数、时间、温度）
- 关键技巧点明
- 列举常见失败原因与解决
- 风格：亲切、专业
- 严禁套话

【风格要求 - 严格执行】
- 像真人写的一样：口语化、有个人见解、敢下判断、不绕弯子
- 严禁套话："在当今时代"、"随着...的发展"、"值得注意的是"、"综上所述"、"总而言之"、"首先...其次...最后..."、"不可否认"、"毋庸置疑"
- 严禁空话开头：不要"在这个"、"近年来"、"众所周知"
- 允许使用：第一人称"我"、"我们"、反问句、短句、偶尔的口语词
- 段落长度参差不齐：有的 2-3 句，有的 5-8 句
- 数字和事实要具体，不要"很多"、"一些"、"大量"等模糊词

【内容完整性】
- 把每个观点讲透：背景、原理、例子、对比、注意点都要说清楚
- 不要因为怕长而省略关键论述
- 也不要注水或重复表达
- 整体保持中等篇幅（1500-3000 字），既充实又不冗长

【输出格式 - 严格执行】
- 输出严格的 JSON 格式（不要用 Markdown 代码块包裹），结构如下：
  {
    "title": "吸引人、不标题党的标题",
    "content": "## 章节标题\n\n正文段落...\n\n## 下一章节\n\n..."
  }
- content 字段是 Markdown 正文，用 ## 分章节（3-5 个），用空行分段
- 字段名必须是 content（不要写成 body / text / markdown / 其它任何名字）
- 适合配图的位置插入 [[IMG: 具体场景描述]] 标记（数量不限，只要合适就插）；不需要可忽略
- 不要任何开场白，不要解释，直接输出 JSON`,

  imagePrompt: `为美食文章"{{topic}}"生成 {{count}} 个诱人菜品图提示词。

要求：
- 食物特写、俯拍、45 度
- 风格：明亮、诱人、干净背景
- 避免道具过多

JSON 输出：["prompt1", ...]`,
};

export const educationPrompts: CategoryPrompts = {
  outline: `你是教育内容创作者。基于主题生成学习/教育文章大纲。

主题：{{topic}}
关键词：{{keywords}}
目标平台：{{platform}}

要求：
- 3-5 个章节，方法论清晰
- 包含：原理、方法、案例、误区
- 适合家长/学生阅读
- 适合 {{platform}}

JSON 输出：{"title": "...", "sections": [{"heading": "...", "points": ["..."]}]}`,

  article: `你是教育内容创作者。基于大纲写完整学习/教育文章。

主题：{{topic}}
目标平台：{{platform}}
{{outline_block}}

要求：
- 方法论可复用
- 引用教育学/心理学研究
- 列举常见误区
- 严禁焦虑营销、严禁成功学
- 严禁套话

【风格要求 - 严格执行】
- 像真人写的一样：口语化、有个人见解、敢下判断、不绕弯子
- 严禁套话："在当今时代"、"随着...的发展"、"值得注意的是"、"综上所述"、"总而言之"、"首先...其次...最后..."、"不可否认"、"毋庸置疑"
- 严禁空话开头：不要"在这个"、"近年来"、"众所周知"
- 允许使用：第一人称"我"、"我们"、反问句、短句、偶尔的口语词
- 段落长度参差不齐：有的 2-3 句，有的 5-8 句
- 数字和事实要具体，不要"很多"、"一些"、"大量"等模糊词

【内容完整性】
- 把每个观点讲透：背景、原理、例子、对比、注意点都要说清楚
- 不要因为怕长而省略关键论述
- 也不要注水或重复表达
- 整体保持中等篇幅（1500-3000 字），既充实又不冗长

【输出格式 - 严格执行】
- 输出严格的 JSON 格式（不要用 Markdown 代码块包裹），结构如下：
  {
    "title": "吸引人、不标题党的标题",
    "content": "## 章节标题\n\n正文段落...\n\n## 下一章节\n\n..."
  }
- content 字段是 Markdown 正文，用 ## 分章节（3-5 个），用空行分段
- 字段名必须是 content（不要写成 body / text / markdown / 其它任何名字）
- 适合配图的位置插入 [[IMG: 具体场景描述]] 标记（数量不限，只要合适就插）；不需要可忽略
- 不要任何开场白，不要解释，直接输出 JSON`,

  imagePrompt: `为教育文章"{{topic}}"生成 {{count}} 个学习场景图提示词。

要求：
- 学习场景（书桌、教室、图书馆）
- 风格：明亮、积极、专注
- 避免人物特写

JSON 输出：["prompt1", ...]`,
};

export const ALL_CATEGORIES = [
  { slug: 'ai', name: 'AI 与科技', icon: '🤖', description: 'AI 技术、应用、行业动态', prompts: aiPrompts },
  { slug: 'tech', name: '程序员向', icon: '💻', description: '技术教程、工具、编程实践', prompts: techPrompts },
  { slug: 'mom-baby', name: '母婴育儿', icon: '👶', description: '育儿经验、母婴用品、早教', prompts: momBabyPrompts },
  { slug: 'life', name: '生活百科', icon: '🏠', description: '生活技巧、家居、购物', prompts: lifePrompts },
  { slug: 'career', name: '职场发展', icon: '💼', description: '职业规划、面试、职场关系', prompts: careerPrompts },
  { slug: 'emotion', name: '情感心理', icon: '💗', description: '两性关系、心理、情感故事', prompts: emotionPrompts },
  { slug: 'food', name: '美食烹饪', icon: '🍜', description: '菜谱、探店、美食文化', prompts: foodPrompts },
  { slug: 'education', name: '教育学习', icon: '📚', description: '学习方法、考试、课外辅导', prompts: educationPrompts },
];